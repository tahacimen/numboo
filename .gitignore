# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Capacitor native Android project — generated from www/ via `npx cap add
# android` + `node scripts/apply-android-config.js` (see README). Not committed;
# CI regenerates it on every build.
android/

# Test artifacts
/tmp-screenshots/
*.png.tmp

# npm lockfile (CI uses `npm install`)
package-lock.json
